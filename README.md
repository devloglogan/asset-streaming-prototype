# Asset Streaming Prototype

## Running Test HTTP Server
Install python and run a test HTTP server from the root directory of this project with command:
```
python -m http.server 8080
```

## Controls
- `R`: Begin asset streaming (requires HTTP server running)
- `WASD`: Move player
